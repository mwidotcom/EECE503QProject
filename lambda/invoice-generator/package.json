{
  "name": "shopcloud-invoice-generator",
  "version": "1.0.0",
  "description": "Lambda function: generates PDF invoices from SQS events",
  "main": "src/index.js",
  "scripts": {
    "test": "jest --coverage",
    "package": "zip -r function.zip src/ node_modules/ package.json"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.450.0",
    "@aws-sdk/client-ses": "^3.450.0",
    "pdfkit": "^0.14.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}

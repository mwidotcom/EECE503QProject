apiVersion: v1
kind: Service
metadata:
  name: cart
  namespace: shopcloud
  labels:
    app: cart
spec:
  type: ClusterIP
  selector:
    app: cart
  ports:
    - name: http
      port: 80
      targetPort: 3002

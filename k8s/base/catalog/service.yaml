apiVersion: v1
kind: Service
metadata:
  name: catalog
  namespace: shopcloud
  labels:
    app: catalog
spec:
  type: ClusterIP
  selector:
    app: catalog
  ports:
    - name: http
      port: 80
      targetPort: 3001
      protocol: TCP

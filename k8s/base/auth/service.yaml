apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: shopcloud
  labels:
    app: auth
spec:
  type: ClusterIP
  selector:
    app: auth
  ports:
    - name: http
      port: 80
      targetPort: 3004
